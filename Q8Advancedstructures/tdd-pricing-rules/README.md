# TDD Pricing Rules (Go)

Real-world example demonstrating **functions as values**, **closures**, and **lambdas** in TDD:
- Discount rules as functions
- Closures capture config (thresholds, rates, product IDs)
- Unit tests (AAA style)

## Run
```bash
cd pricing
go test ./...
```
